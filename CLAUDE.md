# ExpandableX

## Agent skills

### Issue tracker

Issues live in GitHub Issues at `vdvman1/ExpandableX`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Uses the canonical label vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
